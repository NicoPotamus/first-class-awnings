services:
  first-class:
    build:
      context: ./client
    image: localhost/first-class-awnings_first-class
    container_name: first-class
    env_file:
      - ./client/.env
    ports:
      - "4000:3000"
    restart: unless-stopped